// const data = new File(["woo"], "war.txt", { type: "text/plain" });
// const possible = navigator.canShare({ files: [data] });
//
// let fragment = document.createDocumentFragment();
// let div = document.createElement("div");
// div.innerHTML = `<h1>possible: ${possible}</h1>`;
// fragment.appendChild(div);
//
// document.body.appendChild(fragment);

import "../css/index.css";
import swapicon from "../icon/swap.svg";

(document.querySelector("img#swap") as HTMLImageElement).src = swapicon;

// const unit: HTMLInputElement = document.querySelector("input.unit");

// function resize() {
//   swap.style.left = `${unit.getBoundingClientRect().right}px`;
// }

// resize();
// window.addEventListener("resize", resize);
